# Contributing to Utilities4Iran
1. All modules must reside in `categories/<category>/<module>`.
2. Every module requires a `README.md`, `SECURITY.md`, and `tests/`.
3. Run `node .github/scripts/generate-catalog.js` before pushing.
